<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jz.bigdata.common.masscanip.dao.IMasscanipDao">
	<!-- 返回结果设置 -->
	<resultMap id="BaseResultMap"
		type="com.jz.bigdata.common.masscanip.entity.Masscanip">
		<id column="id" jdbcType="VARCHAR" property="id" />
		<result column="ip" jdbcType="VARCHAR" property="ip" />
		<result column="date" jdbcType="VARCHAR" property="date" />
	
	</resultMap>

	<!-- 查询字段设置可重复调用 -->
	<!-- 部门id,name:部门名称,level:部门层级,superiorId:上级id,subordinate:是否有下级（1有0没有）,orderId:位置排序,comment:简介 -->
	<sql id="Base_Column_List">
		id, ip,date
	</sql>

	<!-- 查询所有数据 -->
	<select id="selectAll" 
		resultMap="BaseResultMap">
		<!-- <bind name="name" value="'%' + name + '%'" /> -->
		select * from  masscanip
	</select>


	<!-- 添加数据 -->
	<insert id="insert" parameterType="com.jz.bigdata.common.masscanip.entity.Masscanip">
		insert into masscanip
		<!-- 格式化 -->
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<!-- 判断参数是否为空 -->
			<if test="id != null">
				id,
			</if>
			<if test="ip != null">
				ip,
			</if>
			<if test="date != null">
				date
			</if>
		
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="id != null">
				#{id,jdbcType=VARCHAR},
			</if>
			<if test="ip != null">
				#{ip,jdbcType=VARCHAR},
			</if>
			<if test="date != null">
				#{date,jdbcType=VARCHAR}
			</if>
			
		</trim>
	</insert>
	<!-- 删除数据 -->
	<delete id="delete" parameterType="java.lang.String">
		delete from masscanip where
		id = #{id,jdbcType=VARCHAR}
	</delete>
	
	<!-- 根据id修改信息 -->
	<update id="updateById" parameterType="com.jz.bigdata.common.masscanip.entity.Masscanip">
		update masscanip set
		ip=#{ip}
		where id=#{id}
	</update>
</mapper>